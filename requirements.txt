# --- CORE DEEP LEARNING ENGINE ---
# PyTorch and TorchAudio are required to run ECAPA-TDNN, SpEx+, and KWT-1
torch>=2.2.0
torchaudio>=2.2.0

# --- STAGE 1: NOISE SUPPRESSION ---
# The official package for DeepFilterNet
deepfilternet>=0.5.6

# --- STAGE 2 & 3: SPEAKER ISOLATION & DETECTION ---
# SpeechBrain is the best open-source library for running ECAPA-TDNN easily
speechbrain>=1.0.0

# --- AUDIO PROCESSING & MATH ---
# Libraries for reading .wav files and manipulating audio arrays
numpy>=1.26.0
scipy>=1.12.0
librosa>=0.10.1
soundfile>=0.12.1

# --- UTILITIES ---
# Hugging Face Hub allows your scripts to automatically download the pre-trained models
huggingface_hub>=0.21.0
# Tqdm gives you nice loading bars in the terminal so you know the AI is thinking
tqdm>=4.66.0

# --- STAGE 3: CUSTOM WORD DETECTION ---
faster-whisper>=1.0.0